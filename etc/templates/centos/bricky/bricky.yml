bricky:
  name: myproject
  images: bricky/images
  bricks:
    mounts:
      source: .
      ignore:
        - source/vendor/bundle
    dotenv:
      source:
        - .env
    bundle:
      cache: true
      without: development:test
    centos:
      build: bricky/build
      auto_generate_spec: true
