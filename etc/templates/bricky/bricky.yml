bricky:
  name: myproject
  images: bricky/images
  bricks:
    mounts:
      source: .
      ignore:
        - source/log
        - source/tmp
        - source/vendor/bundle
    bundle:
      cache: true
      without: development:test
    debian:
      build: bricky/build
